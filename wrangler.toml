name = "kuma-kare"
main = "worker/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Local dev uses values from .dev.vars (gitignored).
[vars]
ALLOWED_EMAILS = "caretaker@example.com"

# Routes / site configuration for production deployment.
# [[routes]]
# pattern = "kuma.example.com/*"
# custom_domain = true

# Secrets (set via `wrangler secret put`):
# - JWT_SECRET
# - ANTHROPIC_API_KEY
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET
